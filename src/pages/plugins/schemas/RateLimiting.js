export default {
  'config-policy': {
    label: 'Config.Policy',
    type: 'select',
    default: 'redis',
    values: ['local', 'redis'],
  },
  'config-strategy': {
    label: 'Config.Strategy',
    type: 'select',
    default: 'redis',
    values: ['local', 'redis'],
  },
}
