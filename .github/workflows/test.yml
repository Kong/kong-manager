name: Test

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
  pull_request:

jobs:
  e2e-tests-oss:
    name: E2E Tests - OSS
    uses: ./.github/workflows/.reusable_e2e_tests_oss.yml
    with:
      gateway-image: kong/kong-dev:3.9.1-rc.2
    secrets: inherit
